data_info:
  name: "FAVC"  # Choose the dataset used: FAVC (FakeAVCeleb), AV1M (AV-Deepfake1M), AVLips
  root_path: "path/to/features"
  csv_root_path: "csv_metadata/favc"
  apply_l2: True  # Apply l2 normalization of input features (always used in our experiments)
  num_workers: 12


ckpt_path: "ckpt/AVPF_pretrain.ckpt"  # Path to the

output_path: "outputs/test_results"  # Path to output folder

seed: 42
